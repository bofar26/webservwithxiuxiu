#!/usr/bin/env python3
# Deliberately invalid python. The interpreter exits with an error and prints
# nothing on stdout, so the server has to answer 502 instead of an empty 200.

this is not valid python at all !!!
